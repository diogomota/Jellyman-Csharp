using UnityEngine;
using System.Collections;

public class thrust_lighting_system : MonoBehaviour {
	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		//light.intensity = Random.Range (0.7f,6.5f); //removed  for performance
	}
}
